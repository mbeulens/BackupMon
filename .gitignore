# Real config & MySQL credentials (only the .example files are committed)
BackupCheck.config.psd1
mysql-creds.cnf
# Runtime state (created on the server, never committed)
backupcheck.state.json
# Local test scratch
TestResults/
*.log
.remember/
